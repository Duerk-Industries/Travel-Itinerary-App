DATABASE_URL=postgres://postgres:postgres@localhost:5432/travel
AUTH_SECRET=replace-with-strong-secret

# Optional mail settings for invites
SMTP_HOST=
SMTP_PORT=
SMTP_FROM=
SMTP_USER=
SMTP_PASS=

# Optional AI itinerary generation
OPENAI_API_KEY=
