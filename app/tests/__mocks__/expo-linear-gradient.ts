// Inert host-tag stand-in, same approach as react-native-svg.ts — tests only need
// LinearGradient to render its children, not to actually paint a gradient.
const LinearGradient = 'LinearGradient';

export default LinearGradient;
export { LinearGradient };
