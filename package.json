{
  "name": "shared-trip-planner",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "lint": "echo \"Add linters per workspace\"",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "canvas": "^3.2.0",
    "patch-package": "^8.0.1"
  }
}
