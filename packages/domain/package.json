{
  "name": "@wanderbunnies/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Shared pure domain logic for WanderBunnies (status rules, covering rules) consumed by app and server",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "npm run typecheck",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
